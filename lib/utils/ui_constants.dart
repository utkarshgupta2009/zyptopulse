  
import 'package:flutter/material.dart';

class UIConstants {
    
  static const EdgeInsets screenPadding = EdgeInsets.all(16.0);
  static const EdgeInsets contentPadding =
      EdgeInsets.symmetric(vertical: 8.0, horizontal: 16.0);
  static const EdgeInsets screenPaddingLarge = EdgeInsets.all(24.0);
  static const EdgeInsets smallContentPadding =
      EdgeInsets.symmetric(vertical: 8.0, horizontal: 2.0);

    
  static const double cardBorderRadius = 15.0;
  static const double buttonHeight = 56.0;
  static const double iconSize = 24.0;

    
  static const Duration shortAnimationDuration = Duration(milliseconds: 150);
  static const Duration mediumAnimationDuration = Duration(milliseconds: 300);
  static const Duration longAnimationDuration = Duration(milliseconds: 500);

    
  static const double smallSpacing = 8.0;
  static const double extraSmallSpacing = 4.0;
  static const double mediumSpacing = 16.0;
  static const double largeSpacing = 24.0;
  static const double extraLargeSpacing = 32.0;
}
